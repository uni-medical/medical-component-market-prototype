# GitHub Pages deployment

The repository is public and deploys through GitHub Actions. In repository settings choose **Settings → Pages → Source → GitHub Actions**. The workflow installs dependencies in `frontend/`, builds the Vite app, and publishes `frontend/dist`. The project site URL is `https://uni-medical.github.io/medical-component-market-web-homepage/`.
